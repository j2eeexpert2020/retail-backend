# ==============================
# Docker PostgreSQL Configuration
# ==============================

# Connect to PostgreSQL running inside a Docker container
# The hostname 'postgres' is the service name from docker-compose.yml
spring.datasource.url=jdbc:postgresql://postgres:5432/testdb
spring.flyway.url=jdbc:postgresql://postgres:5432/testdb
